<div class="container">
<ul>
  {{#each model as |product|}}
    <li> {{product.id}} {{product.name}}</li>
    <li> {{product.unbrandedName}} {{product.brandedName}}</li>
    <li> {{product.currency}} {{product.priceLabel}}</li>
  {{!--   <li> {{product.instock}} {{product.stock}}</li> --}}
   {{star-rating item=product rating=2.5 on-click="updateRating"}}
  	<li><img src="{{product.image.sizes.Best.url}}" alt=""></li>
    {{/each}}

</ul>
</div>